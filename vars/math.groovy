
def sum(a,b)
{
  echo "This sum of {a} and {b} is {a+b}"
}
def sub(a,b)
{
  echo "This substraction result of {a} and {b} is {a-b}"
}
def mul(a,b)
{
  echo "This multiplication result of {a} and {b} is {a*b}"
}
def mul(a,b)
{
  echo "This divide result of {a} and {b} is {a/b}"
}
